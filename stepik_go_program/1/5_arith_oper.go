package main

// Что бы получить float, то хотя бы один операнд должен быть флоатом
// % возвращает остаток от деления. Допустим только для целочисленных значений
// x++ инкремента
// x-- дикремента

func main() {
	a := 100
	b := 10

	var m float32 = 10.0 / 6
	c := a + b // с = 110

	c = a * b // с = 1000

	c = a - b // с = 90

	c = a / b // с = 10
}
